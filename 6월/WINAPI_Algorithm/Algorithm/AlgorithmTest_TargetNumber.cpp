#include <string>
#include <vector>

using namespace std;

vector<int> nums;
vector<int> results;

int FindTarget(int it, int len) {
    if (it == len) return 0;
    
}

int solution(vector<int> numbers, int target) {
    int answer = 0;
    return answer;
}

int main() {
    nums = { 1, 4, 7, 2 ,3, 6, 5, 7 };
    results = vector<int>(nums.size(), 0);

    return 0;
}